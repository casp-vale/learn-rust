fn main() {

    // both are macros in Rust, 
    // anything ending with a ! mark is a macro, 
    // think of macros as code that writes other code.
    println!("Hello World"); // appends a new line character (\n) at the end of the text
    print!("Hello World"); // doesn't append  a new line
}